postgis:
  image: mdillon/postgis:9.5
  ports:
    - "5432:5432"
  volumes:
    - "/var/lib/postgresql/data:/pg_data"
  environment:
    - POSTGRES_PASSWORD=mysecretpassword
webapp:
  image: jaykayone/busdelaymap
  ports:
    - "80:80"
